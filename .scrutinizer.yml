checks:
    javascript: true

build:
    environment:
        node: '7.10'

    tests:
        override:
            -
                command: 'npm run test'
                coverage:
                    file: 'coverage/clover.xml'
                    format: 'clover'
